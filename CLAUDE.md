# CLAUDE.md

Use `AGENTS.md` as the canonical repository guide for current patterns,
commands, and known limitations.

Do not assume roadmap/spec items are implemented until the code and tests prove
they are. When this file and `AGENTS.md` disagree, update this file to point
back to `AGENTS.md` rather than duplicating guidance.
